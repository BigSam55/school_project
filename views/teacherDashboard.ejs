<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/dashboard.css">
    <title>Dashboard</title>

</head>
<body>
    <div class="dashboard">
        <div class="sidebar">
            <a href="#"><i class="fas fa-home"></i> Dashboard</a>
            <a href="#"><i class="fas fa-book"></i> Courses</a>
            <a href="#"><i class="fas fa-user"></i> Profile</a>
            <a href="/forgetpassword"><i class="fas fa-cog"></i> Edit Password</a>
            <a href="#"><i class="fas fa-cog"></i> Settings</a>
            <form method="post" action="/logout">
                <button id="logoutbtn" type="submit">Logout</button>
              </form>
            <!-- Add more links with icons as needed -->
        </div>
        <div class="wel" style="width:20% ;"> <h1 style="font-size: 1.3rem; padding: 10px; color:rgb(34,79,116); margin-top: 2px; margin-left: 10px;">Welcome, <p style="color: rgb(242,89,28); display: inline-block;"><%=user.username%> !..</p> </h1></div>
       
    
        <input type="text" name="" id="" placeholder=" Search anything here..." style="width: 50%; height: 28px; margin-top: 15px; border: solid 2px rgb(250, 248, 248); border-radius: 50px; padding: 5px; margin-left:30px;font-family: serif;">
    
        <div class="details" style="display: block; position:static;width: 200px; height: 350px; background-color:rgb(34,79,116); padding: 20px; color: white;  font-size: 1rem; border-radius: 20px;  margin-top: 100px; font-family: serif; margin-right: 10px;">
    
            <img style="width: 80px; height: 80px; margin-left: 50px; border-radius: 100px;" src="data:<%= user.image.contentType %>;base64,<%= user.image.data.toString('base64') %>" alt="student passport">
            <h3 style="text-align: center;"><%= user.fullname %></h3>
            <h4 style="text-align: center;"><%=user.role  %></h4>
            <p style="color:rgb(242,89,28); text-align: center;">Contact Details</p>
            <h5>Email: <%= user.email %></h5>
            <h5>Phone: <%= user.phone %></h5>
        </div>

    </div>

    <div class="student">

        <h3 style="color:rgb(242,89,28) ;">Your Students</h3>

        <h4 style="color: red; text-align: center; font-size: 1.1rem;"><%= messages %></h4>
        
       
        <table>
            <thead>
                <tr>
                    <th>Names</th>
                    <th>Profiles</th>
                    <th>Upload Results</th>
                    <th>Delete</th>
                  </tr>
            </thead>
        <tbody>
           
            <% for( let index = 0; index < user.students.length; index++ ) { %>
                <tr>
                <th><%= user.students[index].Fullname%></th>
                <th><a href="/View_profile/<%= user.students[index]._id%>"><button>View Profile</button></a> </th>
                <th> <a href="/result"><button>Upload Result</button></a></th>
                <th> <a href="/deleteStudent?teacherId=<%= user._id %>&studentId=<%= user.students[index]._id %>"><button>Delete</button></a></th>
                

            </tr>
            <% } %>
        </tbody>
        </table>
    
       </div>


    
    
   
   
</body>
</html>

